//React.renderComponent( //Warning: React.renderComponent will be deprecated in a future version. Use React.render instead.
/** @jsx React.DOM */
React.render(
    <h1>Hello, world!</h1>,
    document.getElementById('my-div')
);
//render has two args: the component we want to render, and the DOM node it should mount to. 